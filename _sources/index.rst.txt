欢迎来到 PyNetIM 文档！
========================

PyNetIM 是一个用于网络影响力最大化和扩散建模的 Python 库。

.. toctree::
   :maxdepth: 2
   :caption: 使用指南
   :hidden:

   installation
   quickstart
   examples

.. toctree::
   :maxdepth: 2
   :caption: API 文档
   :hidden:

   autoapi/index

功能特性
--------

* 🚀 多种影响力最大化算法
* 🔥 扩散模型实现，支持自定义扩展
* 🖇️ 图结构封装
* ⚡ 支持单进程和多进程模式
* 🛠️ 算法性能计时器装饰器

快速开始
--------

安装
~~~~

.. code-block:: bash

   pip install pynetim

基本使用
~~~~~~~~

.. code-block:: python

   import networkx as nx
   import pynetim as im
   
   # 创建图
   g = nx.erdos_renyi_graph(100, 0.2, seed=42)
   graph = im.IMGraph(g, edge_weight_type='WC')
   print(graph)
    
   # 使用度折扣算法获取种子集
   k = 10
   dd = im.algorithms.DegreeDiscountAlgorithm(graph)
   seeds = dd.run(k)
   print(seeds)
   
   # 在IC模型上进行传播模拟
   ic = im.IndependentCascadeModel(graph, seeds, record_states=False)
   influence_spread = im.run_monte_carlo_diffusion(ic, 1000, seed=42)
   print(influence_spread)

索引和搜索
==========

* :ref:`genindex`
* :ref:`modindex`
* :ref:`search`