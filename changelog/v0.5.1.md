# v0.5.1 更新日志

**发布日期**: 2026-04-10

---

## 概述

v0.5.1 是一个重要版本，新增了评估指标模块和时间测量工具，统一了参数命名，增加了多线程参数验证，并新增了7个经典启发式算法。

---

## 🎯 新增功能

### 评估指标模块 (evaluation)

新增完整的评估指标模块，提供影响力最大化算法的多种评估指标：

#### 排名指标 (ranking_metrics)
- `kendall_tau` - Kendall's Tau 系数，评估两个排名的相关性
- `spearman_correlation` - Spearman 相关系数
- `monotonicity_score` - 单调性得分，评估算法区分节点重要性的能力
- `ranking_distance` - 排名距离（支持 Kendall、Spearman、Hamming）
- `ranking_stability` - 多次运行排名的稳定性

#### 影响力指标 (influence_metrics)
- `average_shortest_distance` - 种子节点之间的平均最短距离（支持权重）
- `top_k_accuracy` - Top-K 准确率，评估预测种子与真实种子的重叠
- `top_k_overlap` - 两个排名在 Top-K 位置的重叠率

#### 种子质量指标 (seed_quality_metrics)
- `neighbor_coverage` - 邻居覆盖率
- `degree_statistics` - 度统计信息（均值、最大、最小、标准差）
- `degree_distribution` - 度分布
- `mean_centrality` - 平均中心性
- `seed_overlap` - 种子集重叠率（Jaccard）
- `seed_diversity` - 种子多样性
- `weight_statistics` - 权重统计
- `clustering_coefficient` - 聚类系数

#### 网络指标 (network_metrics)
- `distribution_entropy` - 分布熵
- `local_clustering` - 局部聚类系数
- `reachability` - 可达性

**使用示例**:
```python
from pynetim import IMGraph
from pynetim.evaluation import (
    kendall_tau,
    monotonicity_score,
    top_k_accuracy,
    average_shortest_distance,
    degree_statistics,
)

graph = IMGraph(edges, weights)

# 评估排名相关性
tau, p = kendall_tau(ranking1, ranking2)

# 评估单调性（区分度）
mono = monotonicity_score(importance_scores)

# 评估 Top-K 准确率
acc = top_k_accuracy(predicted_seeds, ground_truth_seeds, k=10)

# 评估种子节点分布
avg_dist = average_shortest_distance(graph, seeds)
stats = degree_statistics(graph, seeds)
```

### 时间测量模块 (timing)

新增时间测量工具，支持多种测量方式：

- `measure_time` - 装饰器，测量函数运行时间
- `AlgorithmTimer` - 算法计时器类，支持单次和多次运行统计
- `measure_runtime` - 函数式测量运行时间
- `measure_runtime_multiple_runs` - 多次运行统计
- `compare_algorithms_runtime` - 比较多个算法的运行时间

**使用示例**:
```python
from pynetim.timing import measure_time, AlgorithmTimer

# 方式1：装饰器
@measure_time
def my_algorithm(graph, k):
    return seeds

seeds, runtime = my_algorithm(graph, k=10)

# 方式2：AlgorithmTimer
timer = AlgorithmTimer(algorithm)
seeds, runtime = timer.run(k=10, mc_rounds=1000, random_seed=42)

# 多次运行统计
stats = timer.run_multiple(k=10, num_runs=5)
print(f"Mean: {stats['mean']:.4f}s, Std: {stats['std']:.4f}s")
```

### 工具函数 (utils)

新增最短路径计算函数：

- `shortest_path_length` - 计算两节点间最短路径（支持跳数和权重）
- `all_pairs_shortest_path_length` - 计算节点对之间的最短路径
- `renumber_edges` - 重新编号边列表节点

**使用示例**:
```python
from pynetim.utils import shortest_path_length

# 基于跳数
dist = shortest_path_length(graph, source=0, target=5, use_weight=False)

# 基于权重
dist = shortest_path_length(graph, source=0, target=5, use_weight=True)
```

### 启发式算法

新增 7 个经典影响力最大化启发式算法：

| 算法 | 时间复杂度 | 特点 | 参考文献 |
|------|-----------|------|----------|
| **DegreeCentralityAlgorithm** | O(n log n) | 选择度数最高的节点，最简单快速 | 基准算法 |
| **PageRankAlgorithm** | O(n·iter) | 基于随机游走评估节点重要性 | Brin & Page 1998 |
| **VoteRankAlgorithm** | O(n·k) | 投票机制选择分散的影响力节点 | Zhang et al. 2016 |
| **KShellDecompositionAlgorithm** | O(m) | 分层分解找到核心节点 | Kitsak et al. 2010 |
| **BetweennessCentralityAlgorithm** | O(n·m) | 选择最短路径上的"桥梁"节点 | Freeman 1977 |
| **ClosenessCentralityAlgorithm** | O(n·m) | 选择距离其他节点最近的节点 | Sabidussi 1966 |
| **EigenvectorCentralityAlgorithm** | O(n·iter) | 邻居重要的节点更重要 | Bonacich 1972 |

---

## 🔄 API 变更

### 参数重命名

为了避免与种子集 `seeds` 混淆，以下参数进行了重命名：

| 旧参数名 | 新参数名 | 影响范围 |
|---------|---------|---------|
| `rounds` | `mc_rounds` | 所有传播模型、算法 |
| `seed` | `random_seed` | 所有传播模型、算法 |

**示例**:
```python
# 旧版本
model.run_monte_carlo_diffusion(rounds=1000, seed=42)

# 新版本
model.run_monte_carlo_diffusion(mc_rounds=1000, random_seed=42)
```

### 随机种子默认值变更

所有 `random_seed` 参数的默认值改为 `None`，表示每次运行结果不同：

```python
# 每次运行结果不同
model.run_monte_carlo_diffusion(mc_rounds=1000)

# 固定随机种子，结果可重现
model.run_monte_carlo_diffusion(mc_rounds=1000, random_seed=42)
```

### 多线程参数验证

当 `use_multithread=True` 时，`num_threads` 必须大于 0，否则抛出中文错误：

```python
# 错误用法 - 会抛出异常
model.run_monte_carlo_diffusion(
    mc_rounds=1000, 
    use_multithread=True, 
    num_threads=0  # ValueError: 启用多线程时，线程数(num_threads)必须大于0
)

# 正确用法
model.run_monte_carlo_diffusion(
    mc_rounds=1000, 
    use_multithread=True, 
    num_threads=4
)
```

---

## 📦 统一接口

### BaseCallbackDiffusionModel

`run_single_trial` 方法参数统一：

```python
# 旧版本
def run_single_trial(self, seeds, rng_seed):
    ...

# 新版本
def run_single_trial(self, seeds, random_seed):
    ...
```

### BaseMultiprocessDiffusionModel

`run_single_trial` 和 `run_monte_carlo_diffusion` 方法参数统一：

```python
# 旧版本
def run_single_trial(self, seeds, rng_seed):
    ...

def run_monte_carlo_diffusion(self, num_trials, num_processes=None):
    ...

# 新版本
def run_single_trial(self, seeds, random_seed):
    ...

def run_monte_carlo_diffusion(self, mc_rounds, num_processes=None, random_seed=None):
    ...
```

---

## 📝 受影响的模块

### 传播模型 (C++ 绑定)

- `IndependentCascadeModel`
- `LinearThresholdModel`
- `SusceptibleInfectedModel`
- `SusceptibleInfectedRecoveredModel`
- `PyDiffusionModelBase`

### 传播模型 (Python)

- `BaseCallbackDiffusionModel`
- `BaseMultiprocessDiffusionModel`

### 算法

- `GreedyAlgorithm`
- `CELFAlgorithm`
- `CELFPlusAlgorithm`

### 类型存根文件 (.pyi)

- `independent_cascade_model.pyi`
- `linear_threshold_model.pyi`
- `susceptible_infected_model.pyi`
- `susceptible_infected_recovered_model.pyi`

---

## 🐛 错误信息

新增中文错误信息：

| 错误场景 | 错误信息 |
|---------|---------|
| 多线程时线程数 <= 0 | `启用多线程时，线程数(num_threads)必须大于0` |

---

## ✅ 测试覆盖

新增测试脚本：

- `test_evaluation.py` - 评估指标和时间测量工具测试
- `test_params.py` - 参数重命名和多线程验证测试
- `test_heuristic.py` - 启发式算法测试和性能对比

测试覆盖：

- 所有评估指标函数测试
- 时间测量工具测试
- IC/LT/SI/SIR 模型参数验证
- Greedy/CELF 算法参数验证
- Python 回调模型参数验证
- BaseCallbackDiffusionModel 参数验证
- BaseMultiprocessDiffusionModel 参数验证
- 多线程验证测试
- random_seed=None 随机性测试
- 所有新增启发式算法测试

---

## 📚 文档更新

- 更新所有示例代码中的参数名
- 更新类型提示文件 (.pyi)
- 更新 API 文档
- 新增评估模块文档
- 新增时间测量模块文档
- 新增启发式算法文档和参考文献

---

## 升级指南

### 从 v0.5.0 升级

1. 将 `rounds` 参数改为 `mc_rounds`
2. 将 `seed` 参数改为 `random_seed`
3. 如果使用自定义传播模型，更新 `run_single_trial` 方法签名
4. 确保多线程模式下 `num_threads > 0`
5. 评估指标现在通过函数调用，不再需要实例化类
